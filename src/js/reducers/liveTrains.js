//TODO: Skip this file in the review, an example for my self going forward
const liveTrains = (state = {}, action) => {
  switch (action.type) {
    case "UPDATE_LIVE_TRAINS_FROM_API":
    //Todo
    default:
      return state;
  }
};

export default liveTrains;
